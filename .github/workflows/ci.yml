name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest hypothesis
          pip install -e ./bindings/python

      - name: Run Python tests
        run: |
          pytest ./bindings/python/tests

      - name: Run Rust tests
        run: |
          cd bindings/rust
          cargo test

      - name: Build C++ tests (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cmake -S bindings/cpp -B bindings/cpp/build -DCMAKE_BUILD_TYPE=Debug
          cmake --build bindings/cpp/build
          ./bindings/cpp/build/integration_test
          ./bindings/cpp/build/cpp_unit_tests
          ./bindings/cpp/build/cpp_fuzz_test
          ./bindings/cpp/build/cpp_error_test

      - name: Build C++ tests (Windows)
        if: runner.os == 'Windows'
        run: |
          cmake -S bindings/cpp -B bindings/cpp/build
          cmake --build bindings/cpp/build --config Debug
          .\bindings\cpp\build\Debug\integration_test.exe
          .\bindings\cpp\build\Debug\cpp_unit_tests.exe
          .\bindings\cpp\build\Debug\cpp_fuzz_test.exe
          .\bindings\cpp\build\Debug\cpp_error_test.exe

      - name: Build C tests (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cmake -S bindings/c -B bindings/c/build -DCMAKE_BUILD_TYPE=Debug
          cmake --build bindings/c/build
          ./bindings/c/build/c_integration_test
          ./bindings/c/build/c_error_test

      - name: Build C tests (Windows)
        if: runner.os == 'Windows'
        run: |
          cmake -S bindings/c -B bindings/c/build
          cmake --build bindings/c/build --config Debug
          .\bindings\c\build\Debug\c_integration_test.exe
          .\bindings\c\build\Debug\c_error_test.exe
